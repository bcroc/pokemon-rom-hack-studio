# NDS Extension Plan

PokemonHackStudio remains one Swift/macOS app with `PokemonHackCore` and `pokemonhack-cli`. NDS support extends the existing source-tree-first workbench instead of becoming a separate editor.

## Clean-Room Posture

- Local `.nds` files are user-supplied inputs and stay ignored.
- Central NDS references under `/Users/bryan/projects/reference-repos` are read-only research unless a future license review approves reuse.
- Small, stable binary containers can be native Swift parsers with synthetic fixtures only: NDS header, FNT/FAT NitroFS index, overlay table metadata, banner facts, and NARC member listing.
- Complex rebuild/conversion systems stay external-tool or planning-only at first: NDS graphics conversion, 3D Nitro formats, audio, script compilation, compression/recompression, overlay rebuilds, and ROM writes.
- Future NDS writes must use mutation-plan previews, backups, source hash checks where source-backed, and ignored `.pokemonhackstudio/` outputs.

## Phased Roadmap

1. **Done - NDS ROM Resource Inspector V1**: platform/profile detection for `.nds`, read-only header facts, NitroFS file/folder listing, overlay counts, NARC member listing, CLI JSON, Resources/Project Hub visibility, synthetic tests.
2. **Done - NDS Decomp Project Detection**: detect Diamond/Pearl, Platinum, HeartGold/SoulSilver, and PMD-Sky-style source trees through `rom.rsf`, `filesystem.mk`, `files/` or `res/`, variant `.sha1`, and game directories such as `platinum.us`, `heartgold.us`, and `soulsilver.us`.
3. **Done - Gen IV Read-Only Data Catalog**: source/decomp-aware personal/species, moves, items, trainers, encounters, text, scripts, and map resource indexes with diagnostics, shallow counts, source spans, CLI JSON, and Resources visibility where available.
4. **Done - NARC-Backed Read-Only Summaries**: source-tree and ROM-backed NARC containers now appear in NDS data catalogs with member counts, sample member paths, diagnostics, and Resources metadata; Diamond/Pearl unpacked archive directories are summarized without reconstruction.
5. **Done - NDS Toolchain Health**: report user-installed devkitPro/BlocksDS, ndstool, grit, mmutil, devkitARM, ndspy-compatible tooling, melonDS/DeSmuME, reference-only DSPRE/Tinke posture, NDS header facts, and source-tree build prerequisites as optional external dependencies.
6. **Done - Source-Backed Record Editing**: local source-tree NDS data records in safe UTF-8 formats now draft, preview, apply, discard, back up, and reload through the mutation-plan contract in CLI and Resources.
7. **Done - Semantic Editor Design V1**: eligible Platinum source-backed JSON species, move, item, trainer data, direct trainer-class JSON, direct encounter JSON top-level and existing slot scalar values, direct field-event JSON, direct map-matrix JSON, direct area-data JSON, existing source text lines under `res/text/**/*.txt`, existing source text JSON string leaves under `res/text/**/*.json`, and existing species evolution tuple records, HeartGold/SoulSilver personal/trainer/item JSON, direct item CSV, nested encounter JSON top-level and existing slot scalar values, direct zone-event JSON, and `src/data/map_headers.h` direct designated `sMapHeaders[]` integer literal assignment records, Diamond/Pearl personal/trainer/item JSON records, Diamond/Pearl encounter JSON top-level and existing slot scalar records, direct Diamond/Pearl field-event JSON records, direct Diamond/Pearl area-data JSON records, Diamond/Pearl `arm9/src/itemtool.c` `sItemIndexMappings` integer initializer rows, Diamond/Pearl `arm9/src/trainer_data.c` `sTrainerClassGenderCountTbl` integer initializer rows, and Diamond/Pearl `arm9/src/map_header.c` `sMapHeaders` integer-literal initializer scalars expose scalar semantic fields in Resources and CLI plan/apply commands while still lowering to the source-backed NDS mutation plan. Separate binary-only export plans remain later rows.
8. **Done - Map/Script/Text Readiness**: NDS data catalog rows now carry read-only relationship and readiness metadata for map, matrix, script, text, filesystem manifest, and container review in CLI JSON and Resources facts. External editors, compilers, extraction, rebuilds, NARC packing, mutation apply, and ROM/container writes stay blocked.
9. **Done - Container Member Fingerprints**: NARC and unpacked archive directory summaries now include bounded read-only member path/index, size, extension, leading magic, format/compression hint, confidence, diagnostics, CLI JSON, and Resources facts for future graphics, text, map, and migration routing.
10. **Done - Graphics Preview Metadata**: sampled NDS container members now expose read-only preview metadata for known Nitro graphics-adjacent formats, while compressed, unsupported, too-short, and unreadable members stay blocked and metadata-only.
11. **Done - Text Bank Preview Metadata**: safe source text, JSON message banks, and BMG-like Gen IV text rows now expose read-only decoded string counts, decoded sample strings, blocked writer/export actions, CLI JSON, and Resources facts. BMG/message-bank writer, container, extraction/rebuild, and ROM export behavior remains read-only; the Platinum `.txt` line semantic row is only a source-line convenience over the raw NDS mutation-plan gate.
12. **Done - Extracted Directory Migration Plan**: binary container and NitroFS manifest rows now report read-only source-tree and extracted-directory candidate paths, unsupported preservation/rebuild steps, blocked actions, CLI JSON, and source/ROM Resources facts without materializing extracted files or enabling repack/rebuild/export behavior.
13. **Done - Shared Migration Coverage Diagnostics**: `migration-coverage <path> --json` reports NDS source, ROM, and source-backed Gen IV semantic domains against shared source-first/read-only/migration-plan/binary-only/blocked statuses so PHS-T93 can guide PHS-T92 and future PHS-T98 slices without enabling NDS build/playtest execution or ROM/container writes.
14. **Done - Diamond/Pearl Item Mapping Semantic Fields**: the semantic editor now parses only the existing `sItemIndexMappings` table in Diamond/Pearl `items:arm9/src/itemtool.c`, exposes `itemIndexMappings.<index>.itemDataIndex`, `.iconIndex`, `.paletteIndex`, and `.gen3Index` integer scalar fields, and replaces only initializer values through the existing NDS mutation plan. Other C anchors, non-integer expressions, row add/remove/reorder, binary item rows, NARC/container rows, generated/reference rows, ROM/export/rebuild paths, and binary writes remain blocked/read-only.
15. **Done - Diamond/Pearl Personal JSON Semantic Fields**: Diamond/Pearl source-backed personal JSON records under `files/poketool/personal` and `files/poketool/personal_pearl` now expose existing top-level scalar JSON fields through the same semantic editor and NDS mutation-plan gate. Nested personal arrays/objects, binary personal rows, other Diamond/Pearl C anchors, NARC/container rows, generated/reference rows, ROM/export/rebuild paths, and binary writes remain blocked/read-only.
16. **Done - Diamond/Pearl Trainer JSON Semantic Fields**: Diamond/Pearl source-backed trainer JSON records under `files/poketool/trainer` now expose existing top-level scalar JSON fields through the same semantic editor and NDS mutation-plan gate. Nested trainer arrays/objects, party rows, binary trainer rows, other Diamond/Pearl C anchors, NARC/container rows, generated/reference rows, ROM/export/rebuild paths, and binary writes remain blocked/read-only.
17. **Done - Diamond/Pearl Field Event JSON Semantic Fields**: Diamond/Pearl source-backed direct field-event JSON records under `files/fielddata/eventdata/*.json` now expose existing top-level scalar JSON fields through the same semantic editor and NDS mutation-plan gate. Nested event arrays/objects, nested event directories, map matrices/tables, `arm9` map/script C anchors, non-JSON rows, NARC/container rows, generated/reference rows, ROM/export/rebuild paths, and binary writes remain blocked/read-only.
18. **Done - Diamond/Pearl Trainer Class Gender Semantic Fields**: the semantic editor now parses only the existing `sTrainerClassGenderCountTbl` table in Diamond/Pearl `trainers:arm9/src/trainer_data.c`, exposes `trainerClassGenderCounts.<index>.genderCount` integer scalar fields, and replaces only initializer values through the existing NDS mutation plan. Non-integer expressions, row add/remove/reorder, other trainer C anchors, binary trainer rows, NARC/container rows, generated/reference rows, ROM/export/rebuild paths, and binary writes remain blocked/read-only.
19. **Done - Gen V Manual-Only Source Roots**: Black/White and Black 2/White 2 source-shaped roots now surface manual-only summaries for `Makefile`, `config.mk`, linker configs, shallow `src`/`asm`/`include` roots, and variant marker folders when present. These rows only add preview-only readiness/source-role/action-state facts for CLI JSON, `resource-index`, and Resources; Gen V editors, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
20. **Done - Gen V Data Root Inventory Summary**: Black/White and Black 2/White 2 source-shaped roots now surface a shallow manual-only `data/` summary with `Gen V Source Role = dataInventory` through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving detailed `data/encounters/*` preview rows. Gen V editors, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
21. **Done - Gen V `files/a` Archive Group Inventory Summary**: Black/White and Black 2/White 2 source-shaped roots now surface a shallow manual-only `files/a` summary with `Gen V Source Role = nitroArchiveGroupInventory` through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving detailed `files/a/**` child rows. Gen V editors, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
22. **Done - Gen V Sound And Container Facts**: Black/White and Black 2/White 2 source-shaped roots now surface manual-only source-role/readiness facts for `files/**/*.sdat`, sound-adjacent NARC rows, and bounded container summaries through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving `files/a/**` child rows. These facts do not decode audio, extract/decompress members, pack/rebuild NARCs, run builds/playtests, export ROMs, apply mutations, or write source/binary data.
23. **Done - Gen V Overlay And Disassembly Config Inventory Summary**: Black/White and Black 2/White 2 source-shaped roots now surface shallow manual-only `overlays/` and `ndsdisasm_config/` summaries with `Gen V Source Role = overlayInventory` and `disassemblyConfigInventory` through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving detailed `overlays/**` and `ndsdisasm_config/**` child rows. Gen V editors, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
24. **Done - Gen V Message-Bank Inventory Summary**: Black/White and Black 2/White 2 source-shaped roots now surface a shallow manual-only `files/msgdata` summary with `Gen V Source Role = messageBankInventory` through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving `files/msgdata/**` child rows as read-only `messageBankMetadata`. Gen V editors, parsers, source writes, text-bank previews, semantic editability, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, migration candidates, and binary writes remain disabled.
25. **Done - Gen V `files` Root Inventory Summary**: Black/White and Black 2/White 2 source-shaped roots now surface a shallow manual-only `files` summary with `Gen V Source Role = nitroFSRootInventory` and child-count facts through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving all existing `files/**` child rows. Gen V editors, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
26. **Done - Gen V Variant SHA1 Text Facts**: Present Black/White and Black 2/White 2 `*/rom.sha1` marker rows now classify checksum marker text as `empty`, `invalid`, or `valid` and expose normalized digest facts only for valid SHA1 text through NDS catalog, CLI JSON, `resource-index`, and Resources. This reads marker text only; ROM-byte comparison, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
27. **Done - Gen V Fielddata Inventory Summary**: Black/White and Black 2/White 2 source-shaped roots now surface shallow manual-only `files/fielddata`, `files/fielddata/mapmatrix`, `files/fielddata/maptable`, `files/fielddata/script`, and `files/fielddata/eventdata/zone_event` summaries with Gen V source-role, readiness, blocked-action, action-state, and clean-room posture facts through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving `files/fielddata/**` child rows. Gen V editors, source writes, extraction/decompression, build/playtest execution, NARC packing, ROM export, mutation apply, and binary writes remain disabled.
28. **Done - Gen V Encounter Record Facts**: Black/White and Black 2/White 2 source-shaped `data/encounters/*` rows now surface metadata-only encounter record facts such as source root, route key, format, shallow count, byte count, and parse state through NDS catalog, CLI JSON, `resource-index`, and Resources. These facts do not parse encounter schemas or enable Gen V semantic controls, raw-source writes, extraction/decompression, NARC packing, build/playtest execution, ROM export, mutation apply, or binary writes.
29. **Done - Gen V Message Candidate Facts**: Black/White and Black 2/White 2 source-shaped `files/msgdata/**` rows now surface filename-only message candidate count, extension, candidate-kind, basis, and preview-only posture facts through NDS catalog, CLI JSON, `resource-index`, and Resources while keeping `messageBankInventory`/`messageBankMetadata` roles. These facts do not parse or decode message text and do not enable text-bank previews, semantic controls, source writes, extraction/decompression, NARC packing, build/playtest execution, ROM export, mutation apply, migration candidates, or binary writes.
30. **Done - Gen V Fielddata Related Rows**: Black/White and Black 2/White 2 source-shaped `files/fielddata`, `files/fielddata/mapmatrix`, `files/fielddata/maptable`, `files/fielddata/script`, and `files/fielddata/eventdata/zone_event` roots now share preview-only related-row context through existing catalog `relatedRecords`, `Related Rows`, and `Related Domains` facts in NDS catalog JSON, `resource-index`, and Resources. Child map/script/zone rows remain preview-only and outside this root relationship cluster; Gen V editors, parsing, source writes, extraction/decompression, NARC packing, build/playtest execution, ROM export, mutation apply, and binary writes remain disabled.
31. **Done - Gen V Message Metadata Facts**: Black/White and Black 2/White 2 source-shaped `files/msgdata/**` child rows now surface bounded metadata-only byte counts, UTF-8 text-like non-empty line counts for `.txt`, `.gmm`, and `.str`, numeric bank hints preserving filename digit runs for `.bin`, `.dat`, and `.msg`, and explicit `Gen V Message Decoded Preview = noDecodedPreview` posture through NDS catalog JSON, `resource-index`, and Resources. Gen V text parsers, decoded previews, semantic controls, migration candidates, NARC packing, raw-source writes, build/playtest execution, ROM export, mutation apply, and binary writes remain disabled.
32. **Done - Gen V Encounter Fielddata Message Related Rows**: Black/White and Black 2/White 2 source-shaped `data/encounters/*`, `files/fielddata`, `files/fielddata/mapmatrix`, `files/fielddata/maptable`, `files/fielddata/script`, `files/fielddata/eventdata/zone_event`, `files/msgdata`, and `files/msgdata/**` message-candidate rows now share preview-only related-row context through existing catalog `relatedRecords`, `Related Rows`, and `Related Domains` facts in NDS catalog JSON, `resource-index`, and Resources. Encounter/message membership uses an explicit Gen V context key instead of filename token stems; child rows remain read-only with message metadata/no-decoded-preview and fielddata preview-only facts. Gen V script parsing, text decoding, source writes, extraction/decompression, NARC packing, container rebuild, build/playtest execution, ROM export, mutation apply, and binary writes remain disabled.
33. **Done - Platinum Map Matrix JSON Semantic Fields**: Platinum source-backed direct-child `res/field/matrices/*.json` rows now expose existing top-level scalar JSON fields through the same semantic editor and NDS mutation-plan gate. Nested matrix directories, `res/field/maps/**`, map binaries, map tables, scripts/text, generated/reference/container rows, ROM/export/rebuild/playtest paths, row insertion, nested edits, and binary writes remain blocked/read-only.
34. **Done - Platinum Trainer Class JSON Semantic Fields**: Platinum source-backed direct-child `res/trainers/classes/*.json` rows now expose existing top-level scalar JSON fields through the same semantic editor and NDS mutation-plan gate. Nested trainer-class directories, `res/trainers/resources/**`, non-JSON rows, generated/reference/container rows, ROM/export/rebuild/playtest paths, row insertion, nested edits, and binary writes remain blocked/read-only.
35. **Done - Platinum Text Line Semantic Fields**: Platinum source-backed `res/text/**/*.txt` rows now expose existing line contents as `lines.<zeroBasedIndex>.text` string fields through the same semantic editor and NDS mutation-plan gate. BMG/message-bank writers, NARC/container extraction or rebuild/repack, generated/reference rows, ROM rebuild/export, build/playtest execution, multiline/insert/delete/reorder text edits, and binary writes remain blocked/read-only.
36. **Done - HGSS Map Inventory Metadata**: HeartGold/SoulSilver `files/fielddata/mapmatrix`, `files/fielddata/maptable`, and `src/data/map_headers.h` rows now surface inventory-only source-role, source-provenance, action-state, blocked-action, readiness, and diagnostic facts through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving child rows and related map/script/text context. Semantic editing, binary/container rows, nested map directories, scripts, generated outputs, references, NARC rebuild, ROM rebuild/export, and binary writes remain blocked/read-only.
37. **Done - Platinum Text JSON String Leaf Semantic Fields**: Platinum source-backed `res/text/**/*.json` rows now expose existing string leaves as dot-path fields such as `message`, `messages.intro`, and `messages.0.text` through the same semantic editor and NDS mutation-plan gate. BMG/message-bank binary files, JSON message-bank writers, non-string JSON values, missing leaves, multiline edits, insert/delete/reorder edits, generated/reference/container writes, ROM rebuild/export, build/playtest execution, and binary writes remain blocked/read-only.
38. **Done - Platinum Text Line Row Operations**: Platinum source-backed `res/text/**/*.txt` rows now have a planning-first insert/delete/reorder operation planner and redacted CLI surface that lowers only safe operation drafts to the existing NDS mutation-plan/apply gate. BMG/message-bank files, JSON text rows, NARC/container rows, generated/reference rows, ROM rebuild/export, build/playtest execution, newline-containing inserts, invalid ranges, and binary paths remain blocked/read-only. Focused scratch-path core/CLI proof passed, and current `make validate-nds` passed with 111 selected tests and 0 failures.
39. **Done - Platinum Area Data JSON Semantic Fields**: Platinum source-backed direct-child `res/field/area_data/*.json` rows now expose existing top-level scalar JSON fields through the same semantic editor and NDS mutation-plan gate. Nested area-data directories, `res/field/maps/**`, matrices/events beyond their existing rows, non-JSON rows, nested objects/arrays, generated/reference/container rows, NARC/container rows, ROM/export/rebuild/playtest paths, row insertion, binary writes, and new writer families remain blocked/read-only.
40. **Done - Diamond/Pearl Map Inventory Metadata**: Diamond/Pearl `arm9/src/map_header.c`, `files/fielddata/mapmatrix`, `files/fielddata/maptable`, `files/fielddata/land_data`, and `files/fielddata/areadata` rows now surface inventory-only source-role, source-provenance, action-state, blocked-action, readiness, and diagnostic facts through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving child/member rows. Semantic editing, raw C-anchor writes, map editors, compilers, generated outputs, references, NARC rebuild, ROM rebuild/export, and binary writes remain blocked/read-only.
41. **Done - HGSS Script Sequence Inventory Metadata**: HeartGold/SoulSilver `files/fielddata/script/scr_seq` rows now surface inventory-only source-role, source-provenance, action-state, blocked-action, readiness, and diagnostic facts through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving child rows and related map/script/text context. Script parsing, semantic editing, script binary writes, compiler/rebuild work, generated/reference writes, NARC/container rebuild, ROM rebuild/export, mutation apply, and binary writes remain blocked/read-only.
42. **Done - Resources Workflow Readiness**: App Resources and guided workflow/session state now surface selected NDS raw-source editability, semantic-field availability, draft state, mutation-plan readiness, and blocker reasons from existing editability diagnostics, semantic snapshots, and plan/apply state. Gen V rows, reference roots, containers/NARC, unsafe formats, generated/reference roles, path-safety failures, and missing/non-file/non-UTF8 sources stay blocked; no Gen V editor, extraction/decompression, container rebuild, NARC packing, generated/reference write, build/playtest execution, ROM export, binary write, or new NDS apply path is enabled.
43. **Done - Platinum Map Inventory Metadata**: Platinum `res/field/maps` rows now surface a shallow inventory-only `platinumMapInventory` summary plus recursive `platinumMapMember` child facts through NDS catalog, CLI JSON, `resource-index`, and Resources while preserving shallow counts and same-key matrix/event/script/text context. Semantic editing, nested map writers, extraction, NARC/container work, generated/reference writes, build/playtest execution, ROM export, mutation apply, and binary writes remain blocked/read-only.
44. **Done - Diamond/Pearl Map Header C Scalars**: Diamond/Pearl `arm9/src/map_header.c` `sMapHeaders[]` rows now expose existing integer-literal `struct MapHeader` positions as `mapHeaders.<rowIndex>.<fieldName>` semantic fields through the same C scalar replacement and NDS mutation-plan gate used by prior Diamond/Pearl item/trainer C-anchor rows. Constants, macros, `TRUE`/`FALSE`, `ENCDATA(...)`, nested/compound expressions, bad-shape rows, row add/remove/reorder, map table/matrix/land/area rows, scripts, compilers, generated/reference rows, ROM/export/rebuild paths, and binary writes remain blocked/read-only.
45. **Done - HGSS Map Header C Integer Scalars**: HeartGold/SoulSilver `src/data/map_headers.h` rows now expose existing integer literal assignments in direct designated `sMapHeaders[]` entries as `mapHeaders.<MAP_CONSTANT>.<fieldName>` semantic fields through the existing NDS mutation-plan preview/apply path. Identifiers/macros/booleans, complex expressions, missing fields, duplicate edits, non-designated rows, row insertion/removal/reorder, map matrix/table writes, nested map directories, scripts, generated/reference writes, NARC/container rebuilds, ROM rebuild/export, and binary writes remain blocked/read-only.
46. **Done - HGSS Map Relationship Readiness**: HeartGold/SoulSilver root rows for `files/fielddata/mapmatrix`, `files/fielddata/maptable`, and `src/data/map_headers.h` now share root-level related-row context and ready map readiness through NDS catalog, CLI JSON, `resource-index`, and Resources facts while preserving numbered child map/script/text/zone relationships. Matrix/table semantic editing, matrix/table writers, map editors, nested map editing, script editing, extraction, NARC rebuild, ROM rebuild/export, mutation apply, generated/reference writes, and binary writes remain blocked/read-only.
47. **Done - Diamond/Pearl Move/Encounter C-Anchor Readiness Facts**: Diamond/Pearl `moves:arm9/src/waza.c` and `encounters:arm9/src/encounter.c` now expose read-only future-row source-role/provenance/readiness/action-state/blocker facts, blocked readiness summaries, and warning diagnostics through NDS catalog, CLI JSON, `resource-index`, and Resources. Parser support, semantic fields, planner lowering, C-anchor writers, NARC/container work, generated/reference writes, ROM rebuild/export, and binary writes remain blocked.
48. **Done - Gen V Source Data Domain Inventory Facts**: optional Black/White source-shaped `data/pokemon`, `data/moves`, `data/items`, `data/trainers`, and matching `src/data/...` roots now expose preview-only source data-domain inventory facts through NDS catalog, CLI JSON, `resource-index`, and Resources. Root rows report only domain/root/member/byte/sample-path facts, child rows report only filename/extension/byte/basis/posture facts, and the write posture remains blocked for Gen V proprietary decoding, semantic controls, extraction, NARC packing, build/playtest execution, ROM export, mutation apply, source mutation, and binary writes.
49. **Done - Gen V Source Data Related Rows**: optional Black/White source-shaped `data/pokemon`, `data/moves`, `data/items`, `data/trainers`, and matching `src/data/...` root inventory rows now share preview-only related-row/readiness context through existing `relatedRecords`, `Related Rows`, `Related Domains`, and readiness facts in NDS catalog JSON, `resource-index`, and Resources. Child source-data member rows remain outside this root relationship cluster while preserving source-role/source-data facts; Gen V parsers, decoded previews, semantic controls, raw-source writes, extraction, NARC packing, build/playtest execution, ROM export, mutation apply, and binary writes remain disabled.
50. **Done - Diamond/Pearl Area Data JSON Semantic Fields**: Diamond/Pearl source-backed direct-child `files/fielddata/areadata/*.json` rows now expose existing top-level scalar area-data fields through the same semantic editor and NDS mutation-plan gate. The `files/fielddata/areadata` root, binary members, nested area-data directories, `files/fielddata/land_data`, map matrix/table rows, nested objects/arrays, missing field creation, row add/remove/reorder, generated/reference rows, NARC/container work, ROM rebuild/export/playtest paths, and binary writes remain blocked/read-only.
51. **Done - Gen V Source Data Blocked-Action Facts**: optional Black/White source-shaped `data/pokemon`, `data/moves`, `data/items`, `data/trainers`, and matching `src/data/...` root/member rows now surface explicit preview-only source-data blocked-action facts through NDS catalog JSON, CLI JSON, `resource-index`, and Resources. Roots report `domainInventoryPreviewOnly`, members report `memberMetadataPreviewOnly`, and member rows remain outside broad related-row clusters; Gen V parsers, decoded previews, semantic controls, source writes, extraction, NARC packing, build/playtest, ROM export, mutation apply, and binary writes remain disabled.
52. **Done - Gen V Source Data Variant Coverage Rollups**: optional Black/White source-shaped `data/pokemon`, `data/moves`, `data/items`, `data/trainers`, and matching `src/data/...` root rows now surface preview-only variant coverage facts for fixed `black.us`, `white.us`, `black2.us`, and `white2.us` markers through NDS catalog JSON, CLI JSON, `resource-index`, and Resources. Source-data member rows, unavailable-title rows, and non-source-data roots receive no coverage facts; Gen V parsers, decoded previews, semantic controls, source writes, extraction, NARC packing, build/playtest, ROM export, mutation apply, and binary writes remain disabled.

## V1 Native Parsers

- `NDSROMHeader`: title, game code, maker, unit code, device capacity, ARM9/ARM7 ranges, FNT/FAT ranges, overlay table ranges, banner offset, and checksum fields.
- `NitroFSIndex`: FNT/FAT directory traversal, file IDs, paths, offsets, sizes, category guesses, and malformed range diagnostics.
- `NDSOverlayTableIndex`: ARM9/ARM7 overlay entry counts, file IDs, memory ranges, compression flag facts, and bounds diagnostics.
- `NARCIndex`: `NARC` container with `BTAF`/`BTNF`/`GMIF` blocks, named or unnamed members, offsets, sizes, and malformed block diagnostics.

## V2 Source-Tree Index

- `NDSDecompAdapter`: read-only adapter for pret-style Diamond/Pearl, Platinum, HeartGold/SoulSilver, and PMD-Sky roots.
- `NDSDecompSourceTreeIndex`: reports profile, family, build system, marker files, source roots, NitroFS/file manifest roots, variant outputs, checksum files, generated/build artifact paths, build target metadata, and diagnostics.
- App and CLI visibility is intentionally limited to Resources, Project Hub/project inspection, ROM, Build, and Diagnostics routing. Gen IV editors, source mutation, extraction, rebuild, playtest launch, and binary writes remain disabled.
- Central reference roots under `/Users/bryan/projects/reference-repos/repos` are labeled as reference inputs instead of editable project inputs.

## V3 Data Catalog

- `NDSDataCatalogBuilder`: read-only catalog builder for NDS source-tree profiles. It emits project-relative records for Gen IV data domains, shallow JSON/CSV/text counts, byte counts, source spans, diagnostics, and read-only posture.
- Gen V `data/encounters/*` rows stay preview-only and expose only metadata facts such as encounter source root, route key, format, shallow count, byte count, and parse state; no Gen V encounter parser, semantic editor, or write/apply path is enabled.
- Platinum gets the richest semantic path coverage through `res/pokemon`, `res/battle/moves`, `res/items`, `res/trainers`, `res/field`, `res/text`, and `generated/*.txt`; Platinum `res/field/maps` is cataloged as inventory-only map context, not semantic map editing.
- HeartGold/SoulSilver and Diamond/Pearl start with source/path counts, NitroFS-backed paths, C source anchors, and NARC placeholders; HGSS map matrix/table rows and Diamond/Pearl map header/matrix/table/land/area rows additionally expose inventory-only source provenance and blocker metadata, exact Diamond/Pearl move/encounter C anchors expose future-row-blocked readiness facts, HGSS map matrix/table/header roots share review readiness relationships, and HGSS `src/data/map_headers.h` adds limited semantic integer-scalar readiness for existing `sMapHeaders[]` literals. PMD-Sky is intentionally reported as a spin-off resource inventory, not a mainline Gen IV RPG schema.
- `pokemonhack-cli nds-data-catalog <path> --json` exposes the catalog. `resource-index` and Resources add `NDS Data ...` rows, but no Gen IV editor modules, rebuilds, extraction, mutation plans, or ROM writes are enabled.

## V4 Container Summaries

- `NDSDataContainerSummary` enriches catalog records with NARC or unpacked-archive-directory member counts, named/unnamed counts, byte counts, sample member paths, read-only posture, and parser diagnostics.
- Platinum `res/prebuilt/**/*.narc`, HeartGold/SoulSilver `files/**/*.narc`, Diamond/Pearl unpacked `narc_*` archive directories, and ROM-backed NitroFS NARCs all flow through the existing `nds-data-catalog` and `resource-index` surfaces.
- PMD-Sky remains spin-off inventory only. Complex formats, decompression, extraction, rebuilds, data decoding, editor routing, and binary writes remain deferred.

## V5 Toolchain Health

- `ToolchainHealthMatrixBuilder` now adds NDS-specific preview rows for devkitPro/DEVKITARM roots, devkitARM tools, `ndstool`, `grit`, `mmutil`, Python/`ndspy`, BlocksDS Docker, melonDS, DeSmuME, and reference-only DSPRE/Tinke roots.
- NDS source trees and `.nds` ROM inputs reuse `pokemonhack-cli toolchain-health <path> --json` and the existing Build/Patch/Playtest health surface. Missing NDS tools are warnings, not write blockers, because NDS rebuilds and playtest launches remain disabled.
- Build/Patch/Playtest groups NDS health rows into build SDKs, packaging/inspection, Python/ndspy-compatible tooling, manual emulators, reference tools, headers, and declared outputs; sidebar setup actions copy manual commands or paths without adding new settings.
- PokemonHackStudio only detects local tool paths, app bundles, common install roots, and reference-checkout availability. It does not install packages, inspect/pull/run Docker images, run builds, launch emulators, extract assets, or write ROM outputs.

## V6 Source-Backed Record Editing

- `NDSDataMutationPlanner` and `NDSDataMutationApplier` draft catalog rows through the shared mutation-plan contract, with source path, SHA1 and byte-count freshness checks, diagnostics, explicit apply, atomic writes, and backups under `.pokemonhackstudio/backups/`.
- Editable rows are limited to local source-tree JSON, CSV, plain text, C source, and C header files that parse as UTF-8. JSON drafts must parse before a plan is applyable.
- `pokemonhack-cli nds-data-edit-plan <project> <record-id> --draft-file <path> --json` exposes a redacted report that preserves path, preview, hash, byte-count, diagnostic, mutation-plan, and backup metadata without emitting raw replacement bytes. `nds-data-edit-apply <project> <record-id> --draft-file <path> --json` remains a thin adapter over the same guarded core applier.
- NDS raw source drafts persist through the shared project workspace save/autosave system, and selected app catalogs are cached per source fingerprint so large Platinum/HGSS catalogs are not rebuilt by repeated Resources/store state reads.
- Source-backed NDS edit plans warn that generated outputs and rebuild artifacts may be stale after source edits until an external rebuild refreshes them.
- Resources shows a raw NDS Data Record editor only for eligible selected rows, with Preview, Apply, Discard, mutation-plan panel integration, readiness facts for raw/semantic source, draft, and mutation-plan state, and blocked-state messages for ineligible NDS rows. Preview and Apply controls remain tied only to the existing NDS data mutation-plan gates.
- ROM-backed rows, `.nds` files, NARC containers, unpacked archive directories, generated/reference rows, PMD-Sky spin-off inventory, extraction, rebuilds, emulator launch, ROM export, and binary writes remain blocked/read-only.

## V7 Semantic Field Editing

- `NDSDataSemanticEditor` adds the first semantic layer above raw source text by detecting top-level scalar fields in eligible Platinum source-backed JSON records for species/personal/move-style data, item data under `res/items/**/*.json`, trainer data under `res/trainers/data/**/*.json`, direct trainer-class JSON rows under `res/trainers/classes/*.json`, direct encounter JSON rows under `res/field/encounters/*.json` including existing scalar-array slots such as `swarms.0` and object-array slot scalars such as `land_encounters.0.level`, direct field-event JSON rows under `res/field/events/*.json`, direct map-matrix JSON rows under `res/field/matrices/*.json`, direct area-data JSON rows under `res/field/area_data/*.json`, existing source text lines under `res/text/**/*.txt`, existing source text JSON string leaves under `res/text/**/*.json`, existing species evolution tuple method/parameter/target values under `res/pokemon/<species>/data.json`, HeartGold/SoulSilver personal/trainer/item JSON rows under `files/poketool/personal/**/*.json`, `files/poketool/trainer/**/*.json`, and `files/itemtool/itemdata/**/*.json`, direct HeartGold/SoulSilver item CSV rows under `files/itemtool/itemdata/*.csv`, nested HeartGold/SoulSilver encounter JSON rows under `files/fielddata/encountdata/**/*.json` including existing scalar-array slots such as `swarms.1` and object-array slot scalars such as `slots.0.rate`, direct HeartGold/SoulSilver zone-event JSON rows under `files/fielddata/eventdata/zone_event/*.json`, HeartGold/SoulSilver `src/data/map_headers.h` direct designated `sMapHeaders[]` integer literal assignments, Diamond/Pearl personal JSON rows under `files/poketool/personal/**/*.json` and `files/poketool/personal_pearl/**/*.json`, Diamond/Pearl trainer/item JSON rows under `files/poketool/trainer/**/*.json` and `files/itemtool/itemdata/**/*.json`, Diamond/Pearl encounter JSON rows under `files/fielddata/encountdata/**/*.json` including existing scalar-array slots such as `swarms.0` and object-array slot scalars such as `slots.0.rate`, direct Diamond/Pearl field-event JSON rows under `files/fielddata/eventdata/*.json`, direct Diamond/Pearl area-data JSON rows under `files/fielddata/areadata/*.json`, Diamond/Pearl `arm9/src/itemtool.c` `sItemIndexMappings` integer initializer scalars, Diamond/Pearl `arm9/src/trainer_data.c` `sTrainerClassGenderCountTbl` integer initializer scalars, and Diamond/Pearl `arm9/src/map_header.c` `sMapHeaders` integer-literal initializer scalars.
- Semantic edits preserve the source file shape by replacing only the selected scalar value, then flow through `NDSDataMutationPlanner`/`NDSDataMutationApplier` for preview, source hash/size checks, explicit apply, and backups.
- Semantic eligibility diagnostics are carried into the lowered mutation plan so direct CLI/app apply paths cannot bypass the row-specific semantic path policy.

## V8 Text Line Row Operations

- `NDSDataTextLineOperationPlanner` adds planning-first insert/delete/reorder operations for already editable local Platinum `res/text/**/*.txt` rows. Operations are zero-based and sequential; insert may append at the current line count, and reorder destinations are interpreted after the source line is removed.
- Safe line-operation drafts lower to the existing `NDSDataEditPlan` and `NDSDataMutationApplier` path for diagnostics, source hash/size freshness checks, explicit apply, atomic writes, and backups. If eligibility or operation diagnostics contain errors, the planner returns a blocked mutation plan with no source changes.
- `pokemonhack-cli nds-data-text-lines-plan` reports operation kinds/indexes, line counts, diagnostics, redacted change metadata, mutation-plan state, and backup root without emitting inserted text, replacement bytes, or full edited text. `nds-data-text-lines-apply` is a thin adapter over the same guarded plan.
- BMG/message-bank files, JSON text rows, NARC/container rows, generated/reference rows, ROM rebuild/export, build/playtest execution, newline-containing inserts, invalid ranges, and binary paths remain blocked/read-only.
- `pokemonhack-cli nds-data-semantic-plan <project> <record-id> --set <field=value> --json` and `nds-data-semantic-apply ...` expose field-level planning/apply without introducing a separate write path.
- Resources renders semantic field controls above the raw text editor when a selected NDS data row is eligible; ineligible records keep the existing raw editor or read-only blocked state.
- Evolution add/remove/reorder, `res/trainers/resources/**`, nested trainer-class directories, nested trainer/item/personal/event arrays or objects outside the explicitly exposed scalar tuple values, unsupported Platinum encounter scalar arrays/missing arrays/empty arrays/nested row values/broad schema reshaping, HGSS/DP encounter slot insert/delete/reorder, missing slot creation, and nested object reshaping, unsupported nested encounter arrays/objects for Diamond/Pearl rows, nested field-event directories, nested map-matrix directories, nested area-data directories, map tables, `res/field/maps/**`, non-`.txt`/non-`.json` Platinum text rows, non-string Platinum text JSON values, BMG/message-bank binary files, JSON message-bank writers, multiline/insert/delete/reorder text edits, nested HeartGold/SoulSilver CSV item rows, HGSS non-CSV/binary item rows, non-JSON encounter/event rows, HGSS map matrix/table inventory rows, HGSS map header identifiers/macros/booleans/non-integer or complex expressions/non-designated rows/row insertion or removal or reorder, nested HGSS map directories, Diamond/Pearl map header non-integer/boolean/constant/macro/compound-expression values, Diamond/Pearl map matrix/table/land/unsupported area-data inventory rows, Diamond/Pearl move/encounter C anchors, HGSS/DP trainer-class equivalents, other Diamond/Pearl C anchors, Diamond/Pearl non-integer item mapping or trainer class gender expressions, binary item/personal/trainer/map/script data, ROM-backed rows, NARC/container rows, generated/reference rows, PMD-Sky, extraction, NARC/container rebuild or repack, build/playtest execution, binary writes, ROM rebuild, and ROM exports remain blocked/read-only.

## V8B Item CSV Row Operations

- `NDSDataItemCSVRowOperationPlanner` adds planning-first insert/delete/reorder operations for existing local HeartGold/SoulSilver direct-child `files/itemtool/itemdata/*.csv` data rows. Operations are zero-based over data rows only; the header row is parsed for column count and preserved.
- Insert accepts exactly one raw CSV data-row string, rejects newlines/multiline cells, requires parseable CSV, and requires the inserted row to match the existing header column count. Delete/reorder operate only on existing data rows.
- Safe item CSV row-operation drafts lower to the existing `NDSDataEditPlan` and `NDSDataMutationApplier` path for source hash/size freshness checks, explicit apply, atomic writes, and backups. Blocked drafts preserve the original source text and return a non-applyable mutation plan with diagnostics.
- `pokemonhack-cli nds-data-item-csv-rows-plan` reports operation kinds/indexes, row counts, diagnostics, redacted change metadata, mutation-plan state, and backup root without emitting inserted CSV row text, replacement bytes, or full edited text. `nds-data-item-csv-rows-apply` is a thin adapter over the same guarded plan.
- Nested CSV, JSON item rows, binary item rows, NARC/container rows, generated/reference rows, BMG/message banks, non-HGSS profiles, ROM rebuild/export, build/playtest execution, nested schema reshaping, multiline CSV cells, invalid ranges, and binary paths remain blocked/read-only.

## V8C Gen IV Encounter JSON Row Operations

- `NDSDataEncounterJSONRowOperationPlanner` adds planning-first insert/delete/reorder operations for existing local Platinum direct-child `res/field/encounters/*.json` top-level object arrays such as `land_encounters` and local HeartGold/SoulSilver and Diamond/Pearl `files/fielddata/encountdata/**/*.json` top-level object arrays such as `slots`. Operations are zero-based and sequential; reorder destinations are interpreted after removing the source row.
- Insert accepts exactly one raw JSON object row, rejects raw newlines, requires scalar-only values, and requires the inserted object to match the existing row key set. Delete/reorder operate only on bounded existing object rows.
- Safe encounter JSON row-operation drafts lower to the existing `NDSDataEditPlan` and `NDSDataMutationApplier` path for source hash/size freshness checks, explicit apply, atomic writes, and backups. Blocked drafts preserve the original source text and return a non-applyable mutation plan with diagnostics.
- `pokemonhack-cli nds-data-encounter-json-rows-plan` reports record ID, array key, operation kinds/indexes, inserted field count, row counts, diagnostics, redacted change metadata, mutation-plan state, and backup root without emitting inserted JSON row text, replacement bytes, `textPreview`, or full edited text. `nds-data-encounter-json-rows-apply` is a thin adapter over the same guarded plan.
- Scalar arrays such as `swarms`, missing or empty arrays, object rows with mismatched or duplicate keys, nested row values, nested array-key paths such as `metadata.slots`, C anchors, non-JSON rows, generated/reference/container rows, ROM rebuild/export/playtest, broad schema reshaping, and binary paths remain blocked/read-only.

## V8 Map/Script/Text Readiness

- `NDSDataCatalogRecord` now includes `relatedRecords` and a read-only `readiness` summary so existing `nds-data-catalog`, `resource-index`, and Resources rows can show Gen IV context without a new editor module.
- Relationship keys connect same-name map, matrix, script, and text rows where source-tree paths make that safe; HGSS map matrix/table/header root rows share explicit inventory review keys, and Gen V encounter/fielddata/message context rows (`data/encounters/*`, exact fielddata roots, `files/msgdata`, and `files/msgdata/**`) share an explicit Gen V context key instead of filename token stems; unmatched rows remain partial and explain that no same-key context was found.
- HeartGold/SoulSilver map matrix/table directory summaries, `files/fielddata/script/scr_seq` directory summaries and child rows, Diamond/Pearl `files/fielddata/mapmatrix`, `files/fielddata/maptable`, `files/fielddata/land_data`, and unsupported `files/fielddata/areadata` summary/member rows, exact Diamond/Pearl `moves:arm9/src/waza.c` and `encounters:arm9/src/encounter.c` C anchors, plus Platinum `res/field/maps` summary/member rows, expose source-role/provenance/action-state facts and profile-specific write-blocked diagnostics as inventory-only or future-row metadata. Direct-child Diamond/Pearl `files/fielddata/areadata/*.json` rows carry semantic JSON scalar readiness. HeartGold/SoulSilver `src/data/map_headers.h` carries the same map provenance facts plus the V7 integer-scalar semantic gate for existing direct designated `sMapHeaders[]` literals; Diamond/Pearl `arm9/src/map_header.c` carries the V7 integer-scalar semantic gate for existing `sMapHeaders[]` literals.
- Filesystem manifests and binary/container rows report manual-only or blocked readiness with explicit blocked actions for extraction, decompression, compilers, NARC rebuild, ROM rebuild, and ROM export.
- Resources surfaces this metadata as facts and searchable row context. Navigation still routes through existing Resources behavior; no editor, compiler, extraction, rebuild, mutation apply, NARC packing, ROM export, or binary/container write path is added by this pass.

## V9 Container Member Fingerprints

- `NDSDataContainerSummary` now carries `NDSDataContainerMemberFingerprint` rows for sampled NARC and unpacked archive members.
- Fingerprints report path/index, byte count, extension, leading magic bytes/ASCII when safe, conservative Nitro/text/container/compression hints, confidence, and diagnostics.
- Fingerprinting reads only bounded leading byte windows and caps samples with the existing container sample limit. It does not extract, decompress, rewrite, rebuild, apply mutation plans, export ROMs, or materialize member files.
- `nds-data-catalog`, `resource-index`, and Resources facts expose member hints and compression hints while keeping ROM-backed, source-backed container, generated/reference, PMD-Sky, and binary rows read-only or blocked.

## V10 Graphics Preview Metadata

- `NDSDataContainerMemberFingerprint` now carries optional read-only preview metadata for Nitro palette, character graphics, screen map, cell, animation, font, model, and texture candidates detected from extension or leading magic.
- Preview metadata is descriptive only: it reports ready/blocked status, format, summary, blocked actions, and diagnostics. It does not decode pixels, parse dimensions, extract members, decompress data, convert formats, rebuild containers, apply mutation plans, export ROMs, or materialize preview files.
- Compressed candidates, unsupported formats, too-short members, and unreadable member bytes are explicitly blocked so future graphics, text, map, and migration rows can route the member safely without implying write or conversion support.
- `nds-data-catalog`, `resource-index`, and Resources facts surface preview hints and blocked preview counts consistently for source-tree and ROM-backed container rows.

## V11 Text Bank Preview Metadata

- `NDSDataCatalogRecord` carries optional `NDSDataTextBankPreview` metadata for Gen IV text rows.
- Safe source-backed text, JSON, CSV, C source, and C header rows report decoded string counts and capped sample strings as read-only facts; JSON message-bank rows sample decoded string leaves rather than JSON syntax.
- BMG-like binary text rows use bounded printable-string sampling and explicit blocked diagnostics instead of extraction, conversion, write, rebuild, or export behavior.
- CLI `nds-data-catalog`, CLI `resource-index`, and Resources facts surface the same preview status, decoded string counts, sample strings, and blocked actions; the local Platinum source-tree smoke proves these facts through Resources rows.
- Text bank preview remains read-only for BMG/message-bank writers, containers, extraction, rebuild/repack, ROM rebuild/export, generated/reference rows, build/playtest execution, and binary writes. Platinum `.txt` line semantic edits and `.json` string-leaf semantic edits are narrow source-text conveniences over the raw NDS mutation-plan gate; multiline replacements plus insert/delete/reorder text edits remain blocked.

## V12 Extracted Directory Migration Plan

- `NDSDataCatalogRecord` carries optional `NDSDataMigrationPlan` metadata for binary container and NitroFS manifest rows.
- Migration plans report source-tree candidate paths, extracted-directory candidate paths, unsupported preservation/rebuild steps, blocked actions, and read-only diagnostics.
- Candidate routing uses conservative path/domain heuristics from existing NDS catalog classification; it does not copy reference schemas, inspect external tools, or infer write eligibility.
- CLI `nds-data-catalog`, CLI `resource-index`, and Resources facts surface the same migration status, candidate paths, unsupported steps, and blocked actions for source-tree and ROM rows.
- Migration plans do not extract ROM files, unpack NARCs, materialize member files, repack containers, rebuild ROMs, export ROMs, or apply mutation plans.

## Next Useful Pass

Expand semantic Gen IV coverage one dedicated source-backed domain at a time, continuing from the completed Platinum rows including direct trainer-class JSON, direct map-matrix JSON, direct area-data JSON, source text-line fields, source text JSON string leaves, encounter slot scalars, and encounter JSON object-row operations, HGSS personal/trainer/item/encounter/zone-event JSON slices, HGSS encounter slot scalars, HGSS direct item CSV cells and row operations, HGSS map inventory metadata, HGSS map-header C integer scalars, Diamond/Pearl personal/trainer/item/encounter/field-event/area-data JSON slices, Diamond/Pearl encounter slot scalars, Diamond/Pearl map inventory metadata, and the first Diamond/Pearl item/trainer/map-header C-anchor slices toward other HGSS or Diamond/Pearl source-backed schemas only when they fit the existing mutation-plan architecture. Keep container/ROM writes disabled until dedicated parser, preservation, and rebuild rows exist, and keep NDS build/playtest as manual guidance until a future implementation row opens runnable execution. Future Gen V work remains preview-only source inventory until dedicated parser/planner/apply rows explicitly land.
